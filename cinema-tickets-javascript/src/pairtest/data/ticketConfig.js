const TICKET_CONFIG = {
  ADULT: 25,
  CHILD: 15,
  INFANT: 0,
};

// freeze data properties of object to prevent value changes once imported.
Object.freeze(TICKET_CONFIG);

export { TICKET_CONFIG };
